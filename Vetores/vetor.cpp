#include <iostream>
#include <vector>


using namespace std;

int main(void){
    int n;

    cout "quantos inteiros?"
    cin >> n;

    int v[n];

   for (int i = 0, i<n, i++){

    cin >> v[i];
   }

   for (int i = n-1; i >= 0; i--){

   cin >> v[i];
    }


    cout << "\n";

    return 0;
}